import 'package:flutter/material.dart';

LinearGradient allColor() {
  return LinearGradient(
      colors: [Color(0xff0ABFBC), Color(0xff0ABFBC),Color(0xff0ABFBC)],
      begin: Alignment.topCenter,
      end: Alignment.bottomCenter);
}
